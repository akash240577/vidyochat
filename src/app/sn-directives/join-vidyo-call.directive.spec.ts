import { JoinVidyoCallDirective } from './join-vidyo-call.directive';

describe('JoinVidyoCallDirective', () => {
  it('should create an instance', () => {
    const directive = new JoinVidyoCallDirective();
    expect(directive).toBeTruthy();
  });
});
